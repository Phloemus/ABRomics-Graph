## DO not delete this folder or virtuoso won't work

Virtuoso uses this folder to store the .ttl files used in the graph (it stores it temporarly. But the graph-instance is still mendatory for
virtuoso to work)
