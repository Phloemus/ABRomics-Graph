version: "0.1"

services:

  nginx:
    image: nginx:1.19-alpine
    container_name: abromics-kg-nginx
    ports:
      - 8081:80
    volumes:
      - ./src/nginx-config/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./src/web:/usr/share/nginx/html
    depends_on:
      - virtuoso

  virtuoso:
    image: tenforce/virtuoso:1.3.1-virtuoso7.2.2
    container_name: abromics-virtuoso
    environment:
      SPARQL_UPDATE: "true"
      DEFAULT_GRAPH: "http://sparql.abromics.fr/"
    volumes:
      - ./src/data/virtuoso:/data
      - ./src/graph:/usr/local/virtuoso-opensource/share/virtuoso/vad
      - /dev/null:/usr/local/virtuoso-opensource/share/virtuoso/vad/old-data # delete the old data folder in the container so it doesn't load the old data
    ports:
      - "8890:8890"

  fastapi:
    container_name: abromics-graph-api
    build:
      context: ./src/web/api
    ports:
      - 8080:8080
  
  streamlit:
    image: phloemus/streamlit:0.0.1-sparql
    container_name: streamlit
    volumes:
      - ./src/web/streamlit/app:/streamlit/app
    ports:
      - "8501:8501"
